datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL") // INTERNAL Railway
}

generator client {
  provider = "prisma-client-js"
}

model Subscriber {
  id           String   @id @default(cuid())
  phone        String
  telegramNick String
  discordNick  String
  bitgetUid    String
  email        String   @unique
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
}

  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt
}
